server :
    port : 80 #指定启动端口号

netty : 
   port : 8090
    
#spring :
  #thymeleaf :
  #  prefix : /temp
   # suffix : .html
 # activemq :
   #    broker-url : tcp:http://129.211.12.197:8161
    #   user : admin
      # password : admin
      # enabled : true
      # pool :
        #  max-connections : 100
         # maxAttempts : 99
     #  jms:
        # pub-sub-domain: true
      # hostname : 129.211.12.197
      
    #   redis:
      #   database : 0
     #    timeout : 0
      # Redis服务器地址
      #   host : 127.0.0.1
      # Redis服务器连接端口
     #    port : 6379
      # Redis服务器连接密码（默认为空）
      #   password : root
      # 连接池最大连接数（使用负值表示没有限制）
       #  pool :
        # max-active : 8
      # 连接池最大阻塞等待时间（使用负值表示没有限制）
       #  max-wait : -1
      # 连接池中的最大空闲连接
      #   max-idle : 8
      # 连接池中的最小空闲连接
      #   min-idle : 0

           
            